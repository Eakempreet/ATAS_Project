# Project notes and personal vaults
ATAS_Notes/

# Local python environment
env/

# Large data folder (keep metadata CSV tracked)
data/
!data/aircraft_metadata.csv

# Logs, artifacts and model binaries
logs/
models/
release_models/
# Use the following to include specific release model files in git
!release_models/aircraft_classifier
!release_models/eta
!release_models/hit

# Prediction outputs and training history
predictions/
model_history/

# VS Code workspace config (workspace-specific)
.vscode/